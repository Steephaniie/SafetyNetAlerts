package com.safetynet.safetynetalerts;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;


/**
 * Classe de test principale pour l'application SafetyNetAlerts.
 * Cette classe sert à s'assurer que le contexte de l'application
 * se charge correctement.
 */
@SpringBootTest
class SafetyNetAlertsApplicationTests {

	/**
	 * Test simple vérifiant si le contexte Spring Boot est chargé sans erreur.
	 * Ce test garantit que la configuration de base de l'application est correcte.
	 */
	@Test
	void contextLoads() {
	}
}
